[project]
name = "orthogonal-time-core"
version = "0.1.0"
description = "Reference computational core for Orthogonal Time / Chronometria Ortogonalna"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["core"]
